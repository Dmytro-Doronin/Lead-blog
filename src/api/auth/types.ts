export type LoginType = {
    login: string;
    password: string;
};

export type RegisterTypes = {
    login: string;
    password: string;
    email: string;
};

export type UserType = {
    email: string;
    login: string;
    userId: string;
    deviceId: string;
    imageUrl: string;
};
